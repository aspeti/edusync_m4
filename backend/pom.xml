<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
    <relativePath/>
  </parent>

  <groupId>com.edusync</groupId>
  <artifactId>edusync-backend</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>edusync-backend</name>
  <description>
    EduSync backend - monolito modular (Spring Modulith, module-first) sobre Java 25 LTS
    y Spring Boot 4.1.0. Ver docs/design/DD-UC-001.md y ADR-0008/ADR-0009/ADR-0010/ADR-0011.
  </description>

  <properties>
    <java.version>25</java.version>
    <spring-modulith.version>2.1.0</spring-modulith.version>
    <jjwt.version>0.13.0</jjwt.version>
    <testcontainers.version>2.0.5</testcontainers.version>
    <lombok.version>1.18.46</lombok.version>
    <springdoc.version>3.0.3</springdoc.version>
    <!-- Spring AI 2.0.x: docs oficiales declaran Boot 4.0.x y 4.1.x (ADR-0017 / PR-IMPL-022).
         2.0.0 publica starters alineados a Boot 4.1.0 — mismo parent que este modulo. -->
    <spring-ai.version>2.0.0</spring-ai.version>
    <!-- Auditoría de tests: NFR-013 / AGENTS.md §8.2 — umbral en domain/ + application/ -->
    <jacoco.version>0.8.15</jacoco.version>
    <jacoco.coverage.minimum>0.80</jacoco.coverage.minimum>
    <pitest.version>1.25.9</pitest.version>
    <pitest.junit5.version>1.2.3</pitest.junit5.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.modulith</groupId>
        <artifactId>spring-modulith-bom</artifactId>
        <version>${spring-modulith.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-bom</artifactId>
        <version>${spring-ai.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <!-- Web -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <!-- Persistencia -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <!-- Spring Boot 4.0+ separo la autoconfiguracion de Flyway a este starter
           dedicado (spring-boot-flyway ya no viene incluido en spring-boot-autoconfigure);
           sin el, FlywayAutoConfiguration nunca se activa y las migraciones no corren. -->
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-flyway</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- Seguridad (RBAC/JWT concreto llega en DD-UC-002) -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>

    <!-- Validacion declarativa de DTOs de entrada (Bean Validation / Jakarta) - ADR-0012 -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- Reduccion de boilerplate: sin restriccion en infrastructure/application; en domain/
         restringido al allowlist de ADR-0012 (@Getter/@EqualsAndHashCode/@ToString, nunca
         @Data/@Setter/@Builder publico) -->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <version>${lombok.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- Documentacion viva de la API REST (Swagger UI) - ADR-0012 -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc.version}</version>
    </dependency>

    <!-- Monolito modular - ADR-0011 -->
    <dependency>
      <groupId>org.springframework.modulith</groupId>
      <artifactId>spring-modulith-starter-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.modulith</groupId>
      <artifactId>spring-modulith-starter-test</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- Cliente LLM (solo infrastructure, detras de LlmPort) - ADR-0017 / PR-IMPL-022.
         Auto-config de modelos se desactiva (spring.ai.model.chat=none); AiConfig construye
         ChatClient desde edusync.ai.* para no exigir spring.ai.openai.api-key al usar Ollama. -->
    <dependency>
      <groupId>org.springframework.ai</groupId>
      <artifactId>spring-ai-starter-model-ollama</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.ai</groupId>
      <artifactId>spring-ai-starter-model-openai</artifactId>
    </dependency>

    <!-- JWT (login stateless HS256) - DD-UC-002 -->
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <!-- Test -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Testcontainers 2.x renombro los artefactos con prefijo "testcontainers-" -->
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-junit-jupiter</artifactId>
      <version>${testcontainers.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-postgresql</artifactId>
      <version>${testcontainers.version}</version>
      <scope>test</scope>
    </dependency>

    <!-- TestRestTemplate en Spring Boot 4.1 vive en un modulo separado -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-resttestclient</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-restclient</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <!-- Lombok requiere annotationProcessorPaths explicito: la deteccion implicita de
             procesadores de anotaciones (via classpath) no alcanza en Maven cuando conviven
             varias dependencias con procesadores propios (ej. spring-modulith-apt) -->
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <annotationProcessorPaths>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
              <version>${lombok.version}</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>

      <!-- Cobertura de líneas: report siempre; check en verify (NFR-013) -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <configuration>
          <!-- Solo domain + application cuentan para el umbral (AGENTS.md §8.2 / NFR-013).
               infrastructure/, shared.web DTOs y bootstrap quedan fuera del gate. -->
          <includes>
            <include>**/domain/**</include>
            <include>**/application/**</include>
          </includes>
          <excludes>
            <exclude>**/package-info.class</exclude>
            <exclude>**/domain/*Exception.class</exclude>
            <exclude>**/domain/*Id.class</exclude>
          </excludes>
        </configuration>
        <executions>
          <execution>
            <id>jacoco-prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>jacoco-report</id>
            <phase>test</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
          <execution>
            <id>jacoco-check</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
            <configuration>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <counter>LINE</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>${jacoco.coverage.minimum}</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Mutation testing (perfil `mutation`): no bloquea verify por defecto.
           Clases críticas de dominio: CalculoNotas, Usuario, GestionEscolar. -->
      <plugin>
        <groupId>org.pitest</groupId>
        <artifactId>pitest-maven</artifactId>
        <version>${pitest.version}</version>
        <dependencies>
          <dependency>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-junit5-plugin</artifactId>
            <version>${pitest.junit5.version}</version>
          </dependency>
        </dependencies>
        <configuration>
          <targetClasses>
            <param>com.edusync.academico.domain.CalculoNotas*</param>
            <param>com.edusync.identidad.domain.Usuario</param>
            <param>com.edusync.academico.domain.GestionEscolar</param>
          </targetClasses>
          <targetTests>
            <param>com.edusync.academico.domain.CalculoNotasTest</param>
            <param>com.edusync.identidad.domain.UsuarioTest</param>
            <param>com.edusync.academico.domain.GestionEscolarTest</param>
          </targetTests>
          <mutators>
            <mutator>DEFAULTS</mutator>
          </mutators>
          <outputFormats>
            <outputFormat>HTML</outputFormat>
            <outputFormat>XML</outputFormat>
          </outputFormats>
          <timestampedReports>false</timestampedReports>
          <failWhenNoMutations>true</failWhenNoMutations>
          <!-- Soft gate: subir cuando la suite de dominio sea más agresiva -->
          <mutationThreshold>60</mutationThreshold>
          <coverageThreshold>80</coverageThreshold>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!-- Suite rápida sin Docker/Testcontainers (cobertura domain/application vía unit tests).
         El umbral baja a 70 % porque muchos application services solo se cubren en *IntegrationTest. -->
    <profile>
      <id>unit</id>
      <properties>
        <jacoco.coverage.minimum>0.70</jacoco.coverage.minimum>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
              <excludes>
                <exclude>**/*IntegrationTest.java</exclude>
              </excludes>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>mutation</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-maven</artifactId>
            <executions>
              <execution>
                <id>pitest-mutation</id>
                <phase>verify</phase>
                <goals>
                  <goal>mutationCoverage</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
