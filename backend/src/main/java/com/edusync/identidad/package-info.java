/**
 * Modulo {@code identidad} (Spring Modulith) - Usuario, UsuarioRol, login/JWT, RBAC.
 *
 * <p>Implementa la autenticacion de {@code FSD-UC-021} (Gestion de Usuarios y Roles).
 * Vacio hasta {@code DD-UC-002} (docs/design/DD-UC-002.md), que anade el aggregate
 * {@code Usuario}, el login con JWT, el seed del primer {@code SYSADMIN} y el puerto
 * publico {@code UsuarioCreacionPort}.
 *
 * <p>Bootstrap creado por {@code DD-UC-001} / {@code PR-IMPL-001} (ADR-0011).
 */
package com.edusync.identidad;
