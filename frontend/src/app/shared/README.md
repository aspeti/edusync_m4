# `shared/`

Componentes de UI comunes reutilizables entre features (botones, tablas, formularios de
layout, pipes/directivas transversales). Sin lógica de negocio ni llamadas HTTP propias.

Vacío en este bootstrap (`DD-UC-001` / `PR-IMPL-001`).
